{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "go build -o ./bin/worker ./cmd/worker",
    "dev": "go run ./cmd/worker",
    "test": "go test ./cmd/worker -v"
  }
}
